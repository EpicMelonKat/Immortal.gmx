if attacking = false {
    attacking = true
}

if attacking = true { //check if attacking
    if weapon_type = "melee" { //check weapon type
        
    }
    if weapon_type = "range" {
    
    }
    if weapon_type = "magic" {
    
    }
}
